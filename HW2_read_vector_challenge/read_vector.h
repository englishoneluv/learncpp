#include <vector>
#include <string>

std::vector<int> ReadVector1() {
    /* 
        This function creates a new vector!
        The values are read from console input.

        Example.
        Input:
        3
        1 2 3
        Output:
        [1, 2, 3]
    */
    
}

std::vector<int> ReadVector2(int threshold) {
    /* 
        This function creates a new vector!
        The values are read from console input(Same as previod task).
        All the values lower than threshold are ignored.
        
        Example.
        threshhold = 2
        Input:
        3
        1 2 3
        Output:
        [2, 3]
    */
    
}

std::vector<std::string> ReadVector3() {
    /* 
        This function creates a new vector!
        The values are read from console input.

        Example.
        Input:
        3
        aba caba abacaba
        Output:
        ["aba", "caba", "abacaba"]
    */
    
}

std::vector<int> ReadVector4() {
    /* 
        This function creates a new vector!
        The values are read from console input.
        First the number of rows.
        The the number of numbers in each row.
        Example.
        Input:
        3 2
        1 1
        2 2
        3 3
        Output:
        [1, 1, 2, 2, 3, 3]
    */
    
}